def export(filepath):
    print(f"Writing string to .tex file at {filepath} (TODO)")
    # print()
    # print(f"Exporting to .tex file at {filepath}")
    # all_results = _res_cache.get_all_results()
    # for _, result in all_results:
    #     result.print()
